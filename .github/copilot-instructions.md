# Repo rules

- Prefer vertical slices (FE+BE+DB) behind feature flags when needed.
- Keep PRs under ~400 changed lines excluding generated files.
- If a change crosses a boundary (API, persistence, auth), add integration/contract tests.
- Always update PR template sections.
