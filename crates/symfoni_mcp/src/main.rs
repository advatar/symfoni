use anyhow::Result;

fn main() -> Result<()> {
    // This is a minimal stub to keep the repository buildable.
    // Implement a full MCP server by exposing symfoni commands over stdio.
    println!("symfoni-mcp stub: MCP server not implemented yet.");
    Ok(())
}
