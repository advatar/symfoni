# macOS dashboard scope (future)

The CLI is canonical. A macOS app is optional.

Allowed scope:
- repo health overview
- onboarding checklist
- launch CLI actions

Out of scope:
- code editing
- auto-merging
- embedding LLMs
