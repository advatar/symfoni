# Individual onboarding (15 minutes)

1. Install `symfoni`
2. Run `symfoni init --preset swift-rust`
3. Run `tools/verify.sh`
4. Use Codex CLI with `.codex/repo-instructions.md` and Claude Code with `CLAUDE.md`
